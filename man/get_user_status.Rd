% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_status.R
\name{get_user_status}
\alias{get_user_status}
\title{Get user online status}
\usage{
get_user_status(user_id, verbose = FALSE, auth = get_default_auth())
}
\arguments{
\item{user_id}{Character. The user ID to query. The API also accepts
`"me"` to refer to the authenticated user.}

\item{verbose}{Logical. Print detailed request information? Default `FALSE`.}

\item{auth}{A `mattermost_auth` object created by
[authenticate_mattermost()]. Defaults to [get_default_auth()].}
}
\value{
A named list with fields `user_id`, `status`, `manual`,
  `last_activity_at`, and `active_channel`.
}
\description{
Retrieve the current status of a user (online, away, offline, or dnd).
}
\section{Permissions}{

Must be authenticated.
}

\examples{
\dontrun{
  status <- get_user_status("user123")
  status$status
  # "online", "away", "offline", or "dnd"

  # Use "me" for the authenticated user
  my_status <- get_user_status("me")
}
}
