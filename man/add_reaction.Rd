% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reactions.R
\name{add_reaction}
\alias{add_reaction}
\title{Add a Reaction to a Mattermost Post}
\usage{
add_reaction(
  user_id,
  post_id,
  emoji_name,
  verbose = FALSE,
  auth = get_default_auth()
)
}
\arguments{
\item{user_id}{The ID of the user adding the reaction.}

\item{post_id}{The ID of the post to react to.}

\item{emoji_name}{The name of the emoji to react with (e.g.
\code{"thumbsup"}). Colons are stripped automatically, so
\code{":thumbsup:"} also works.}

\item{verbose}{(Logical) If \code{TRUE}, detailed information about the
request and response will be printed.}

\item{auth}{The authentication object created by
\code{\link{authenticate_mattermost}()}. Must be a valid
\code{mattermost_auth} object.}
}
\value{
A named list with the reaction's fields: \code{user_id},
  \code{post_id}, \code{emoji_name}, and \code{create_at}.
}
\description{
Creates a reaction (emoji) on a post.
}
\details{
The caller must have \code{read_channel} permission for the channel the
post is in.
}
\examples{
\dontrun{
auth <- authenticate_mattermost()
me <- get_me(auth = auth)

# React with thumbsup
add_reaction(
  user_id = me$id,
  post_id = "post_abc123",
  emoji_name = "thumbsup",
  auth = auth
)

# Colons are stripped automatically
add_reaction(
  user_id = me$id,
  post_id = "post_abc123",
  emoji_name = ":white_check_mark:",
  auth = auth
)
}
}
