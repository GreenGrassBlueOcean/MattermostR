% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reactions.R
\name{remove_reaction}
\alias{remove_reaction}
\title{Remove a Reaction from a Mattermost Post}
\usage{
remove_reaction(
  user_id,
  post_id,
  emoji_name,
  verbose = FALSE,
  auth = get_default_auth()
)
}
\arguments{
\item{user_id}{The ID of the user whose reaction to remove.}

\item{post_id}{The ID of the post to remove the reaction from.}

\item{emoji_name}{The name of the emoji to remove (e.g.
\code{"thumbsup"}). Colons are stripped automatically.}

\item{verbose}{(Logical) If \code{TRUE}, detailed information about the
request and response will be printed.}

\item{auth}{The authentication object created by
\code{\link{authenticate_mattermost}()}. Must be a valid
\code{mattermost_auth} object.}
}
\value{
A named list with the deletion status as returned by the
  Mattermost API (typically \code{list(status = "OK")}).
}
\description{
Deletes a specific reaction (emoji) that a user previously added to a post.
}
\details{
The caller must be the user who made the reaction or have
\code{manage_system} permission.
}
\examples{
\dontrun{
auth <- authenticate_mattermost()
me <- get_me(auth = auth)

# Remove a thumbsup reaction
remove_reaction(
  user_id = me$id,
  post_id = "post_abc123",
  emoji_name = "thumbsup",
  auth = auth
)
}
}
