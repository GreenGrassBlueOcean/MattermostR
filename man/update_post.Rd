% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_post.R
\name{update_post}
\alias{update_post}
\title{Update (Patch) an Existing Mattermost Post}
\usage{
update_post(
  post_id,
  message = NULL,
  is_pinned = NULL,
  props = NULL,
  verbose = FALSE,
  auth = get_default_auth()
)
}
\arguments{
\item{post_id}{The ID of the post to update. Must be a non-empty string.}

\item{message}{Optional. The new message text (supports Markdown).}

\item{is_pinned}{Optional. Set to \code{TRUE} to pin the post to the
channel, or \code{FALSE} to unpin it.}

\item{props}{Optional. A named list of properties to attach to the post.}

\item{verbose}{(Logical) If \code{TRUE}, detailed information about the
request and response will be printed.}

\item{auth}{The authentication object created by
\code{\link{authenticate_mattermost}()}. Must be a valid
\code{mattermost_auth} object.}
}
\value{
A named list containing the updated post's fields (e.g. \code{id},
  \code{message}, \code{channel_id}, \code{is_pinned}).
}
\description{
Partially updates a post by providing only the fields to change. Omitted
fields are left unchanged. Uses the \code{PATCH} endpoint
(\code{PUT /api/v4/posts/\{post_id\}/patch}).
}
\details{
At least one of \code{message}, \code{is_pinned}, or \code{props} must be
provided. The caller must have \code{edit_post} permission for the channel
the post is in.
}
\examples{
\dontrun{
auth <- authenticate_mattermost()

# Edit a message
update_post(post_id = "abc123", message = "Updated text", auth = auth)

# Pin a post
update_post(post_id = "abc123", is_pinned = TRUE, auth = auth)

# Edit message and pin in one call
update_post(
  post_id = "abc123",
  message = "Important update",
  is_pinned = TRUE,
  auth = auth
)
}
}
\seealso{
[pin_post()] and [unpin_post()] for dedicated pin/unpin endpoints.
}
