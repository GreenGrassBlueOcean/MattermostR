% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pin_posts.R
\name{unpin_post}
\alias{unpin_post}
\title{Unpin a post from its channel}
\usage{
unpin_post(post_id, verbose = FALSE, auth = get_default_auth())
}
\arguments{
\item{post_id}{Character. The ID of the post to unpin.}

\item{verbose}{Logical. Print detailed request information? Default `FALSE`.}

\item{auth}{A `mattermost_auth` object created by
[authenticate_mattermost()]. Defaults to [get_default_auth()].}
}
\value{
A named list with `status = "OK"` on success.
}
\description{
Remove a post's pinned status from the channel. This is the semantic
endpoint for unpinning; the same effect can be achieved via
`update_post(post_id, is_pinned = FALSE)`.
}
\section{Permissions}{

Must be authenticated and have `read_channel` permission for the channel
the post is in.
}

\examples{
\dontrun{
  unpin_post("post_id_abc")
}
}
\seealso{
[pin_post()], [update_post()]
}
