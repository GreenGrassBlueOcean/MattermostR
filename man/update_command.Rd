% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/commands.R
\name{update_command}
\alias{update_command}
\title{Update a slash command}
\usage{
update_command(command_id, body, verbose = FALSE, auth = get_default_auth())
}
\arguments{
\item{command_id}{Character. The command ID to update.}

\item{body}{Named list. The full Command object with updated fields.
Must include required fields like `id`, `team_id`, `trigger`, `url`,
and `method`.}

\item{verbose}{Logical. Print detailed request information? Default `FALSE`.}

\item{auth}{A `mattermost_auth` object created by
[authenticate_mattermost()]. Defaults to [get_default_auth()].}
}
\value{
A named list representing the updated command (same schema as
  [create_command()]).
}
\description{
Update a command definition. The request body should be a complete
Command object (the API replaces the entire definition).
}
\section{Permissions}{

Must have `manage_slash_commands` permission for the team the command
belongs to.
}

\examples{
\dontrun{
  cmd <- get_command("cmd_id_abc")
  cmd$description <- "Updated description"
  update_command("cmd_id_abc", body = cmd)
}
}
\seealso{
[get_command()], [create_command()], [delete_command()]
}
